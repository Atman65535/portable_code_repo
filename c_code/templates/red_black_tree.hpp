// Red Black tree
// The root and leaf is black node
// Never two red node become neighbor
// The path from one node to all downstream black node have the same length
// All new nodes are red node.
// So insert only violate the trait that never two red neighbor.
#pragma once
#include <iostream>
